@model IEnumerable<Cinema.Data.Models.ApplicationUser>

@{
    ViewData["Title"] = "Visitors";
}

<link href="~/css/cardStyle.css" rel="stylesheet" />
<div class="moviesContainer flex-column front-panel">
    <h1>Visitors</h1>
    <table class="rwd-table">
        <tbody>
            <tr>
                <th>Name</th>
                <th>Tickets for: </th>
            </tr>
            @foreach (var visitor in Model)
            {
                <tr class="owner">
                    <td>
                        @string.Format($"{visitor.FirstName} {visitor.LastName}")
                    </td>
                    <td>
                       @* @if (visitor.Tickets.Count > 0)*@
                        {
                            @*@foreach (var ticket in visitor.Tickets)
                            {
                                <p><a asp-controller="Movies" asp-action="Details" asp-route-id="@ticket.Movie.Id"><b>@ticket.Movie.Title</b></a> - @ticket.ForDate.ToString("dd.MM.yyyy") - @ticket.Seat.SeatNumber</p>
                            }*@
                        }
                        else
                        {
                           <i><b>No tickets!</b></i>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>