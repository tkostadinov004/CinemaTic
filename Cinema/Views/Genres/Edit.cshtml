@model Cinema.Models.Genre

@{
    ViewData["Title"] = "Edit genre";
}

<link href="~/css/loginRegisterStyle.css" rel="stylesheet" />
<section class="front-panel">
    <div class="row">
        <h1>Edit genre "@Model.Name"</h1>
        <div class="col-md-4">
            <form class="account" asp-controller="Genres" asp-action="Edit" method="post">
                <hr class="section-line" />
                <div class="text-fields">
                    <div role="list" asp-validation-summary="All" class="text-danger"></div>
                    <div class="form-group">
                        <div class="active-form">
                            <label asp-for="Name"></label>
                            <input asp-for="Name" class="form-control" />
                        </div>
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <button type="submit" asp-route-id="@Model.Id" class="nx-btn primary">Edit</button>
            </form>
        </div>
    </div>
</section>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
